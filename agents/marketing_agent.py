"""Marketing Agent — end-to-end campaign planning + execution."""

from agents.base import BaseAgent


class MarketingAgent(BaseAgent):
    role_blurb = (
        "Marketing lead. Plans full campaigns: positioning, target, channel mix, "
        "content calendar, KPIs. Uses content + research + workflow skills."
    )
