# ===========================================================
# HackKnow AI OS — lean dependency set (Render free tier safe)
#
# Optional heavy extras (voice cloning, fine-tuning, browser
# automation, video) live in requirements-extras.txt — install
# locally with:   pip install -r requirements-extras.txt
# ===========================================================

# --- core async + server ---
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
pyyaml>=6.0
loguru>=0.7
tenacity>=8.2
aiofiles>=23.2
orjson>=3.9
websockets>=12.0

# --- LLM clients (lightweight HTTP) ---
groq>=0.5
google-generativeai>=0.5
cohere>=5.3
mistralai>=0.2
together>=1.1
openai>=1.30
huggingface_hub>=0.23

# --- memory / RAG (Chroma uses bundled embeddings) ---
chromadb>=0.5

# --- research + ecommerce + browser scrape ---
beautifulsoup4>=4.12
duckduckgo-search>=5.3
requests>=2.31
woocommerce>=3.0

# --- data + excel ---
pandas>=2.2
openpyxl>=3.1
plotly>=5.21
numpy>=1.26
pillow>=10.3

# --- docs ingestion for RAG ---
pypdf>=4.0
python-docx>=1.1
